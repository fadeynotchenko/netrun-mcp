{
  "manifest_version": "0.3",
  "name": "netrun",
  "display_name": "Netrun",
  "version": "0.1.0",
  "description": "Publish websites and Telegram bots to Netrun — upload a folder from your editor, get a URL",
  "long_description": "MCP is the common way to connect external services to AI editors. Once Netrun is connected, you tell the agent “publish this bot to Netrun”: it uploads the code folder, asks for a token if the code needs one, waits for the build and gives you the link. Works in Cursor, Claude Code, Codex, Claude Desktop, VS Code and Windsurf. Setup takes a minute and one command.",
  "author": {
    "name": "Netrun",
    "url": "https://netrun.io"
  },
  "homepage": "https://netrun.io/mcp",
  "documentation": "https://github.com/fadeynotchenko/netrun-mcp#readme",
  "support": "https://github.com/fadeynotchenko/netrun-mcp/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/fadeynotchenko/netrun-mcp.git"
  },
  "license": "MIT",
  "keywords": [
    "hosting",
    "deploy",
    "telegram-bot",
    "website",
    "netrun"
  ],
  "privacy_policies": [
    "https://netrun.io/privacy"
  ],
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/index.js"
      ],
      "env": {
        "NETRUN_API_KEY": "${user_config.api_key}"
      }
    }
  },
  "user_config": {
    "api_key": {
      "type": "string",
      "title": "Netrun access key",
      "description": "Create it at netrun.io → MCP. Starts with nru_",
      "sensitive": true,
      "required": true
    }
  },
  "tools": [
    {
      "name": "netrun_publish",
      "description": "Uploads a project folder and puts it online — a new project or a new version of an existing one. Detects the stack (Python, Node, Go, Rust, PHP, static HTML, Docker, docker-compose), asks for missing secrets, waits for the build and returns the URL or the failure reason"
    },
    {
      "name": "netrun_status",
      "description": "Is the project running, its URL, what blocks it and why the last publish failed"
    },
    {
      "name": "netrun_logs",
      "description": "Last lines of the application output — the first place to look when a bot goes silent"
    },
    {
      "name": "netrun_set_secrets",
      "description": "Saves tokens and API keys as environment variables (stored encrypted) and re-publishes so the app picks them up"
    },
    {
      "name": "netrun_control",
      "description": "Start, stop or restart a project"
    },
    {
      "name": "netrun_list_projects",
      "description": "All projects of the account with URLs and state"
    },
    {
      "name": "netrun_whoami",
      "description": "The plan, how many projects are available and how much free running time bots have left"
    }
  ],
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=18.0.0"
    }
  }
}
